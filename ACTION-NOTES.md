# Action Notes

This repository now includes a GitHub Actions workflow for building a debug APK.

Before the workflow can succeed, the repository still needs Gradle Wrapper files:
- gradlew
- gradlew.bat
- gradle/wrapper/gradle-wrapper.properties
- gradle/wrapper/gradle-wrapper.jar
